{
	"private": true,
	"engines": {
		"node": ">=16.18.0"
	},
	"scripts": {
		"build": "pnpm run -r build",
		"build:dev": "tsc --build",
		"clean": "pnpm run -r clean",
		"test": "pnpm run -r test",
		"watch": "tsc --build --watch",
		"format": "prettier --write --list-different \"{*,packages/**/*,.github/**/*}.+(ts|tsx|js|json|yml|md)\""
	},
	"devDependencies": {
		"@jupyterlab/buildutils": "^3.0.0",
		"prettier": "^2.7.1",
		"rimraf": "^2.6.3"
	},
	"volta": {
		"node": "16.18.0"
	}
}
